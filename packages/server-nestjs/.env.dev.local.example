# Local private overrides (do NOT commit real values)
# Copy values from this template into .env.dev.local

# Keep model catalog in repo root (relative to packages/server-nestjs)
AGENT_LLM_CATALOG_PATH=../../llm-catalog.json

# ------------------------------------------------------------
# Provider key mapping from /Users/haoqi/OnePersonCompany/friendsAI/llm-catalog.json
# - zhipu_proxy  -> ZHIPU_PROXY
# - zai          -> ZAI
# - claude_proxy -> CLAUDE_PROXY
# - claude-doge  -> CLAUDE_DOGE
# - gpt-doge     -> GPT_DOGE
# - gemini-doge  -> GEMINI_DOGE
# Rule: uppercase and replace non [A-Z0-9] with "_"
# ------------------------------------------------------------

# provider key: zhipu_proxy
# must match providers.zhipu_proxy.apiKeyEnv in llm-catalog.json
AGENT_LLM_PROVIDER_ZHIPU_PROXY_API_KEY=your_openai_compatible_api_key

# provider key: claude_proxy
# must match providers.claude_proxy.apiKeyEnv in llm-catalog.json
AGENT_LLM_PROVIDER_CLAUDE_PROXY_API_KEY=your_claude_api_key

# provider key: zai
# must match providers.zai.apiKeyEnv in llm-catalog.json
AGENT_LLM_PROVIDER_ZAI_API_KEY=your_zai_api_key

# provider key: claude-doge
# must match providers.claude-doge.apiKeyEnv in llm-catalog.json
AGENT_LLM_PROVIDER_CLAUDE_DOGE_API_KEY=your_claude_doge_api_key

# provider key: gpt-doge
# must match providers.gpt-doge.apiKeyEnv in llm-catalog.json
AGENT_LLM_PROVIDER_GPT_DOGE_API_KEY=your_gpt_doge_api_key

# provider key: gemini-doge
# must match providers.gemini-doge.apiKeyEnv in llm-catalog.json
AGENT_LLM_PROVIDER_GEMINI_DOGE_API_KEY=your_gemini_doge_api_key

# Base URLs are recommended in llm-catalog.json (provider.baseURL).
# You can still override via env when needed:
# AGENT_LLM_PROVIDER_ZHIPU_PROXY_BASE_URL=...
# AGENT_LLM_PROVIDER_ZAI_BASE_URL=...
# AGENT_LLM_PROVIDER_CLAUDE_PROXY_BASE_URL=...
# AGENT_LLM_PROVIDER_CLAUDE_DOGE_BASE_URL=...
# AGENT_LLM_PROVIDER_GPT_DOGE_BASE_URL=...
# AGENT_LLM_PROVIDER_GEMINI_DOGE_BASE_URL=...

# Optional: if you rely on embedding with a separate key, keep this independent too
# LLM_EMBEDDING_PROVIDER=openai-compatible
# LLM_EMBEDDING_MODEL=embedding-3
# LLM_EMBEDDING_API_KEY=your_embedding_api_key
# LLM_EMBEDDING_BASE_URL=https://open.bigmodel.cn/api/paas/v4
