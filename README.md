# FriendsAI

AI-powered relationship management app - 智能社交关系管理应用

## 1. 项目简介

这是一个基于 **AI Native** 理念设计的个人/小团队人脉管理系统。与传统 CRM 不同，它不依赖繁琐的表单输入，而是通过**自然语言交互**（对话录音/笔记）来自动沉淀人脉数据。系统利用大语言模型（LLM）进行信息提取、画像构建，并利用向量数据库实现智能检索和推荐。

### 核心价值
*   **无感输入**：记录即归档，AI 自动提取联系人、事件、待办。
*   **智能辅助**：会前一键生成简报，会后自动整理纪要。
*   **主动推荐**：基于关系深度和时间维度的“行动面板”，通过 AI 建议“今天该联系谁”。

### 产品定位
- **目标用户**：个人、自由职业者、小团队管理者
- **核心场景**：商务人脉维护、客户关系管理、个人社交网络梳理
- **差异化**：AI 原生设计，无需手动填写表单，自然语言即数据

## 2. 项目结构

```
friendsAI/
├── packages/
│   ├── client/              # 前端应用 (Taro 跨平台)
│   │   ├── src/
│   │   └── ...
│   │
│   ├── server-nestjs/       # [NEW] AI 原生后端服务 (NestJS)
│   │   ├── src/
│   │   │   ├── action-panel/    # 行动面板模块
│   │   │   ├── ai/              # AI 核心与向量服务
│   │   │   ├── auth/            # 认证模块 (注册/登录/验证码)
│   │   │   ├── briefings/       # 会前简报模块
│   │   │   ├── contacts/        # 联系人管理
│   │   │   ├── conversations/   # 对话处理与 AI 解析
│   │   │   ├── events/          # 事件管理
│   │   │   ├── entities/        # 数据库实体 (TypeORM)
│   │   │   └── users/           # 用户管理
│   │   ├── .env                 # 环境变量配置
│   │   ├── Dockerfile
│   │   └── package.json
│   │
│   └── server/              # [DEPRECATED] 旧版后端 (Express.js)
│
├── designs/                 # 设计文件 (.pen)
├── docs/                    # 项目文档
├── docker-compose.yml       # 数据库编排 (PostgreSQL + PGVector)
├── package.json             # 根目录工作区配置
└── README.md                # 本文件
```

## 3. 技术栈

### 前端 (packages/client)
- **框架**: Taro 3.6 (跨平台小程序框架，支持 H5/微信小程序/支付宝小程序)
- **UI**: React 18 + taro-ui
- **语言**: TypeScript
- **状态管理**: (待补充)
- **构建工具**: Webpack

### 后端 (packages/server-nestjs)
- **框架**: NestJS 11.x (Node.js + TypeScript)
- **数据库**: PostgreSQL 15 (带 PGVector 扩展)
- **ORM**: TypeORM 0.3.x
- **AI SDK**: OpenAI Node.js SDK + LangChain
- **认证**: Session-based + bcrypt
- **测试**: Jest 30.x + Supertest
- **日志**: Pino
- **API 文档**: Swagger/OpenAPI (通过 @nestjs/swagger)

### 基础设施
- **容器化**: Docker + Docker Compose
- **数据库**: PostgreSQL 15 + PGVector 0.5.x
- **向量存储**: PGVector (用于 Embedding 存储和相似度搜索)

## 4. 数据库模型

核心实体关系：

```
User (用户)
├── contacts: Contact[]         # 用户的联系人
└── conversations: Conversation[] # 用户的对话记录

Contact (联系人)
├── user: User                  # 所属用户
├── briefing: Briefing          # 1:1 会前简报
├── conversations: Conversation[] # 关联的对话
├── events: Event[]             # 关联的事件
└── profile: JSONB              # 灵活的个人画像

Conversation (对话)
├── user: User                  # 所属用户
├── contact: Contact            # 关联的联系人
├── content: text               # 对话内容
├── parsedData: JSONB           # AI 解析结果
└── embedding: vector           # 语义向量

Event (事件)
├── contact: Contact            # 关联的联系人
├── title: string               # 事件标题
├── description: text           # 事件描述
└── embedding: vector           # 语义向量

Briefing (会前简报)
├── contact: Contact            # 1:1 关联的联系人
├── lastSummary: text           # 上次沟通摘要
├── pendingTodos: string[]      # 待办事项列表
├── traits: string[]            # 联系人特征标签
└── suggestion: text            # 建议话题
```

## 5. 功能模块详解

### 🛡️ 认证与用户模块 (Auth & Users)
*   **功能**：处理用户注册、登录、登出、个人信息管理。
*   **机制**：基于 Session 的认证，密码采用 bcrypt 哈希存储。
*   **安全特性**：
    - 验证码机制（支持万能验证码用于开发调试）
    - 密码强度校验
    - Session 过期管理

### 👥 联系人管理 (Contacts)
*   **功能**：联系人全生命周期管理（增删改查）。
*   **特点**：
    - 支持 JSONB 格式的灵活画像（Profile）和标签系统
    - 自动关联对话和事件历史
    - 支持收藏/星标联系人
    - 智能搜索（文本 + 语义向量）

### 💬 对话与事件 (Conversations & Events)
*   **功能**：
    *   **对话**：记录用户输入的自然语言笔记。
    *   **事件**：存储具体的日程和交互记录。
*   **AI 能力**：
    - 所有内容自动生成 Embedding 向量，支持语义搜索
    - 自动提取时间、地点、人物等关键信息
    - 关联到相关联系人

### 🧠 AI 处理器 (Conversation Processor)
*   **核心逻辑**：
    1.  接收自然语言输入（会议记录、聊天笔记等）
    2.  调用 LLM 提取联系人、事件、事实和待办事项
    3.  自动更新或创建关联的实体数据
    4.  生成 Embedding 向量存储
    5.  归档解析结果
*   **输出格式**：结构化 JSON（contacts, events, facts, todos）

### 📝 会前简报 (Briefings)
*   **功能**：在见面前生成“作弊小抄”。
*   **逻辑**：
    - 聚合联系人基础信息
    - 提取最近 5 次对话要点
    - 提取最近 5 个事件
    - LLM 生成简要回顾和话题建议
*   **使用场景**：见面前 5 分钟快速浏览，从容应对

### 🚀 行动面板 (Action Panel)
*   **功能**：首页仪表盘，主动推荐今日联系对象。
*   **待跟进**：
    - 基于最后交互时间排序
    - 识别有未完成待办的联系人
*   **AI 推荐**：
    - 智能分析联系频率和关系深度
    - 推荐“即刻联系”的对象
    - 生成推荐理由和开场白
*   **统计数据**：本周新增联系人、对话数、事件数

## 6. 快速开始

### 前置要求
- **Node.js**: v18+ (推荐 v20 LTS)
- **Docker**: 20.x+ (用于运行数据库)
- **npm**: 9.x+ 或 **pnpm**: 8.x+
- **OpenAI API Key**: 用于 AI 功能

### 环境准备

1. **克隆项目**
   ```bash
   git clone https://github.com/Jcateye/friendsAI.git
   cd friendsAI
   ```

2. **安装依赖**
   ```bash
   # 根目录安装工作区依赖
   npm install
   
   # 或者分别安装
   cd packages/server-nestjs && npm install
   cd packages/client && npm install
   ```

3. **启动基础设施**
   ```bash
   # 在项目根目录运行
   docker-compose up -d
   ```
   - 启动 PostgreSQL (端口 5434)
   - 启用 PGVector 扩展
   - 数据持久化到 Docker Volume

4. **配置后端环境**
   ```bash
   cd packages/server-nestjs
   cp .env.example .env  # 如果存在示例文件
   ```
   
   编辑 `.env` 文件：
   ```env
   # OpenAI API 配置 (必需)
   OPENAI_API_KEY=sk-your-actual-api-key-here
   OPENAI_BASE_URL=https://api.openai.com/v1  # 可选，用于代理
   OPENAI_MODEL=gpt-3.5-turbo  # 或 gpt-4
   
   # 应用环境配置
   NODE_ENV=development
   
   # 调试模式万能验证码 (仅在 development 环境生效)
   UNIVERSAL_VERIFY_CODE=123456
   ```

### 启动开发服务器

1. **启动后端服务**
   ```bash
   cd packages/server-nestjs
   npm run start:dev
   ```
   - API 服务运行在 `http://localhost:3000`
   - Swagger 文档：`http://localhost:3000/api`
   - 热重载已启用

2. **启动前端开发服务器**
   ```bash
   cd packages/client
   npm run dev:h5
   ```
   - H5 版本运行在 `http://localhost:10001`

### 验证安装

1. **检查数据库连接**
   ```bash
   # 查看数据库容器状态
   docker ps | grep friendsai-postgres
   
   # 测试数据库连接
   docker exec -it friendsai-postgres psql -U postgres -d friends_ai_db -c "SELECT 1;"
   ```

2. **测试 API**
   ```bash
   # 测试健康检查接口
   curl http://localhost:3000
   
   # 查看 API 文档
   open http://localhost:3000/api
   ```

3. **运行测试**
   ```bash
   cd packages/server-nestjs
   
   # 运行单元测试
   npm run test
   
   # 运行测试并生成覆盖率报告
   npm run test:cov
   
   # 运行 E2E 测试
   npm run test:e2e
   ```

## 7. 开发指南

### 后端开发

#### 目录结构规范
```
src/
├── entities/          # 数据库实体定义
├── modules/           # 功能模块
│   ├── {module}/
│   │   ├── dto/       # 数据传输对象
│   │   ├── {module}.controller.ts  # API 控制器
│   │   ├── {module}.service.ts     # 业务逻辑
│   │   ├── {module}.module.ts      # 模块定义
│   │   └── {module}.spec.ts        # 单元测试
├── ai/                # AI 服务集成
├── auth/              # 认证模块
└── common/            # 公共工具、拦截器、过滤器
```

#### 常用命令
```bash
# 生成新模块
nest generate module {name}
nest generate controller {name}
nest generate service {name}

# 数据库迁移
typeorm migration:create -n {migration-name}
typeorm migration:run
typeorm migration:revert

# 代码检查
npm run lint
npm run format
```

#### API 开发规范
- 使用 DTO 进行数据验证 (class-validator)
- 使用 Swagger 装饰器生成 API 文档
- 统一返回格式：`{ code: number, data: any, message: string }`
- 错误处理使用全局异常过滤器

### 前端开发

#### 开发模式
```bash
# H5 开发模式
npm run dev:h5

# 微信小程序开发模式
npm run dev:weapp

# 支付宝小程序开发模式
npm run dev:alipay
```

#### 构建发布
```bash
# 构建 H5 生产版本
npm run build:h5

# 构建微信小程序
npm run build:weapp
```

## 8. 部署说明

### 后端部署

#### Docker 部署
```bash
cd packages/server-nestjs

# 构建镜像
docker build -t friendsai-server .

# 运行容器
docker run -d \
  --name friendsai-server \
  -p 3000:3000 \
  --env-file .env \
  friendsai-server
```

#### 环境变量配置
生产环境必需的环境变量：
```env
NODE_ENV=production
OPENAI_API_KEY=sk-...

# 数据库配置
DB_HOST=your-db-host
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your-password
DB_DATABASE=friends_ai_db

# Session 密钥
SESSION_SECRET=your-random-secret-key
```

### 前端部署

#### H5 部署
```bash
cd packages/client
npm run build:h5

# 部署 dist/ 目录到静态服务器
```

#### 小程序部署
1. 执行构建命令生成对应平台代码
2. 使用各平台开发者工具上传
3. 提交审核并发布

## 9. 项目状态与路线图

### ✅ 已完成
- [x] 后端基础架构 (NestJS + TypeORM)
- [x] PostgreSQL + PGVector 集成
- [x] 用户认证模块 (注册/登录/验证码)
- [x] 联系人管理 CRUD
- [x] AI 对话解析服务
- [x] 会前简报生成
- [x] 行动面板推荐
- [x] 向量搜索服务
- [x] 基础测试覆盖

### 🚧 进行中
- [ ] 前端 UI/UX 完善
- [ ] 端到端测试补充
- [ ] 性能优化 (缓存、查询优化)
- [ ] 多租户支持

### 📋 计划中
- [ ] 微信小程序发布
- [ ] 移动端 App (React Native)
- [ ] 数据导入/导出功能
- [ ] 团队协作功能
- [ ] 自动化工作流

## 10. 遗留问题与技术债务

### ⚠️ 已知问题

1. **ConversationProcessorService 单元测试**
   - **状态**: 核心业务逻辑已实现，但单元测试需要重构
   - **原因**: 
     - NestJS 测试模块中复杂的依赖注入
     - Jest spyOn 与 TypeScript 类型推断冲突
     - AI Prompt 字符串匹配问题
   - **建议**: 后续迭代中补充 E2E 测试覆盖核心路径

2. **前端构建优化**
   - **状态**: 开发模式正常，生产构建体积较大
   - **建议**: 配置 Tree Shaking、Code Splitting

3. **数据库索引优化**
   - **状态**: 基础索引已创建，复杂查询待优化
   - **建议**: 根据实际查询模式添加复合索引

## 11. 贡献指南

### 提交规范
- 使用 [Conventional Commits](https://www.conventionalcommits.org/)
- 格式：`type(scope): subject`
- 示例：`feat(contacts): add search by tags`

### 分支管理
- `main`: 主分支，稳定版本
- `develop`: 开发分支，日常开发
- `feature/*`: 功能分支
- `hotfix/*`: 紧急修复分支

## 12. 相关资源

### 文档
- [需求文档 (PDF)](./designs/人脉管理系统.pdf)
- [API 文档 (Swagger)](http://localhost:3000/api) (开发环境)
- [数据库设计文档](./docs/database.md) (待补充)

### 技术参考
- [NestJS 官方文档](https://docs.nestjs.com/)
- [TypeORM 文档](https://typeorm.io/)
- [PGVector GitHub](https://github.com/pgvector/pgvector)
- [Taro 文档](https://docs.taro.zone/)

## 13. 联系与支持

- **项目地址**: https://github.com/Jcateye/friendsAI
- **问题反馈**: [GitHub Issues](https://github.com/Jcateye/friendsAI/issues)
- **开发讨论**: [GitHub Discussions](https://github.com/Jcateye/friendsAI/discussions)

---

## License

Private - 仅供学习和研究使用
